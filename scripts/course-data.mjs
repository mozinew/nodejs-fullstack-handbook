export const course = [
  {w:1,t:'环境、REPL 与第一个程序',o:['区分运行时、语言和包管理器','用 REPL 验证表达式','读懂调用栈'],p:'你接手一个云函数，却不知道代码由谁执行、从哪里开始。',m:'JavaScript 是语言规范；Node.js 是实现运行时；npm 是包管理与脚本入口。程序从模块顶层开始执行，函数只有被调用才进入栈。REPL 适合验证一个小假设，脚本适合保存可重复实验。',j:'JDK、JVM、Maven 与 Java 源码的关系，可类比语言、Node 和 npm，但 Node 不需要先生成 JVM 字节码才能运行普通脚本。',bad:'把 node、npm 和 JavaScript 当成同一个东西，环境报错时只能重装。',crm:'从 `functions/nodejs_20/createCustomer/index.js` 找到 CommonJS 入口和三个参数。',task:'实现姓名归一化：去掉首尾空白并把连续空白压成一个。',sol:"export function solve(input) { return String(input ?? '').trim().replace(/\\s+/g, ' '); }",input:'  Ontic   CRM  ',expected:'Ontic CRM'},
  {w:2,t:'值、类型、变量与控制流',o:['识别七种原始类型','正确选择 const/let','避免隐式转换'],p:'CRM 表单把金额、空字符串和布尔值都作为文本传来，直接判断会出现错误。',m:'变量保存值或对象引用。`const` 禁止重新绑定但不冻结对象；`let` 允许重新赋值。`===` 不做隐式类型转换。业务边界先显式转换，再判断有限值、范围和必填条件。',j:'Java 的变量类型在编译期约束赋值；JavaScript 的类型属于运行时值。TypeScript 稍后提供开发期约束，但不会改变运行时。',bad:'使用 `if (amount)` 判断金额，导致 0 被当成缺失；使用 `==` 让字符串和数字自动相等。',crm:'阅读客户和商机函数里的 `normalizeText`、`Number` 与空值分支。',task:'统计输入数组中 string、number、boolean、null 和 object 的数量。',sol:"export function solve(input) { return input.reduce((r,v) => { const k = v === null ? 'null' : typeof v; r[k]=(r[k]??0)+1; return r; }, {}); }",input:['a',1,true,null,{}],expected:{string:1,number:1,boolean:1,null:1,object:1}},
  {w:3,t:'函数、对象、数组与数据管道',o:['编写纯函数','使用 map/filter/reduce','理解浅复制'],p:'需要把商机记录过滤、转换并按区域求和，同时不能污染原始响应。',m:'函数是一等值。优先让转换函数只依赖参数并返回新值。`map` 等长转换，`filter` 筛选，`reduce` 聚合。对象展开仅浅复制，嵌套值仍共享引用。',j:'Java Stream 与数组方法相似，但 JS 数组可容纳不同类型，且对象没有不可变保证。',bad:'在 map 中修改原记录；把所有循环强行写成 reduce 导致不可读。',crm:'阅读 `dashboardMapper.ts` 的数据归一化和看板聚合。',task:'按 region 汇总有效记录的 amount，忽略非有限数字。',sol:"export function solve(rows) { return rows.filter(r=>Number.isFinite(Number(r.amount))).reduce((a,r)=>{a[r.region]=(a[r.region]??0)+Number(r.amount);return a;},{}); }",input:[{region:'北区',amount:'10'},{region:'北区',amount:5},{region:'南区',amount:'x'}],expected:{'北区':15}},
  {w:4,t:'作用域、闭包、原型、class 与 this',o:['解释词法作用域','用闭包封装状态','判断 this 绑定'],p:'本地 OpenAPI 需要缓存 Token，但缓存不能泄露成任意可改的全局变量。',m:'闭包让函数保留定义位置的变量。模块本身也是作用域，因此模块级缓存只在当前进程实例内有效。class 是原型委托的语法外观；普通函数的 this 由调用方式决定，箭头函数捕获外层 this。',j:'Java 实例方法天然绑定对象；JavaScript 方法被单独取出后可能丢失 this。闭包可替代许多只有一个方法的小类。',bad:'把模块缓存误认为跨实例永久存储；把需要动态 this 的方法改成箭头函数。',crm:'阅读 `local-dev/apaas-openapi.cjs` 的 `tokenCache`。',task:'用闭包生成连续序号，返回调用 n 次的结果数组。',sol:"export function solve(n) { let value=0; const next=()=>++value; return Array.from({length:n},next); }",input:4,expected:[1,2,3,4]},
  {w:5,t:'Map、Set、迭代协议与错误模型',o:['用 Set 去重','用 Map 建索引','设计错误分类'],p:'组织层级可能重复甚至成环，需要快速判断已访问人员。',m:'Set 表示唯一集合，Map 支持任意键并保留插入顺序。二者适合图遍历、分组和缓存。错误要区分输入、权限、依赖和内部错误，并保留 cause；不要用字符串匹配猜错误类型。',j:'对应 Java 的 HashSet/HashMap 和异常层级，但 JS 没有 checked exception。',bad:'每次用数组 includes 做大规模查重；catch 后只返回空数组，掩盖依赖故障。',crm:'阅读 `getLeadersFromEmail` 和 multi leader 函数的去重逻辑。',task:'按首次出现顺序返回唯一 ID，忽略空值。',sol:"export function solve(ids) { return [...new Set(ids.filter(Boolean).map(String))]; }",input:['u1','u2','u1',null,'u3'],expected:['u1','u2','u3']},
  {w:6,t:'日期、JSON、正则与 CLI 阶段项目',o:['理解 JSON 边界','处理日期时区','解析命令行参数'],p:'月报周期横跨月末，错误的本地时区转换会少算一天。',m:'JSON 只保存有限数据类型，Date 会变字符串，undefined 会消失。日期必须声明时区和边界包含规则。正则适合格式识别，不适合解析任意嵌套结构。CLI 从 `process.argv` 接收字符串并负责校验。',j:'Java 的 Jackson 与 java.time 类型更显式；JS Date 内部是时间戳，展示受时区影响。',bad:'用 `new Date("YYYY-MM-DD")` 后混用本地 getter；把 JSON.parse 当业务校验。',crm:'阅读 `queryOpportunitiesByDateRange` 的周期边界。',task:'把 YYYY-MM-DD 校验并转换为 UTC 零点 ISO；非法值抛错。',sol:"export function solve(s) { if(!/^\\d{4}-\\d{2}-\\d{2}$/.test(s)) throw new TypeError('INVALID_DATE'); const d=new Date(s+'T00:00:00.000Z'); if(Number.isNaN(d.valueOf())||d.toISOString().slice(0,10)!==s) throw new TypeError('INVALID_DATE'); return d.toISOString(); }",input:'2026-08-26',expected:'2026-08-26T00:00:00.000Z'},
  {w:7,t:'Promise、async/await 与事件循环',o:['解释 Promise 状态','预测微任务顺序','正确等待异步工作'],p:'批量保存使用 async forEach，函数已经返回成功，后台写入却仍未完成。',m:'async 函数总返回 Promise。await 暂停当前 async 函数并把后续安排为微任务，不会阻塞进程。forEach 不消费回调返回的 Promise；串行用 for...of，并行用 Promise.all。',j:'await 不是 Thread.sleep，也不等于创建线程；更接近把 continuation 注册到 CompletionStage。',bad:'`items.forEach(async item => await save(item))` 后立刻返回。',crm:'阅读 `agent_input` 的轮询与云函数中的 Promise.all。',task:'按输入顺序异步翻倍并返回结果。',sol:"export async function solve(input) { return Promise.all(input.map(async n => n * 2)); }",input:[1,2,3],expected:[2,4,6]},
  {w:8,t:'并发限制、取消、超时与重试',o:['区分并发和并行','设计有限并发','只重试瞬态错误'],p:'同时向飞书发几百个请求，立刻触发 15 QPS 限制。',m:'Promise.all 会立即启动所有已创建任务。生产代码需要队列或分批限制在途数量。超时要能取消底层操作；重试使用指数退避和抖动，只处理幂等的瞬态失败。',j:'类似受界线程池与 Resilience4j，但 Node 的限制对象通常是异步任务而非线程。',bad:'任何错误都重试三次；超时只 reject 外层 Promise却不取消请求。',crm:'结合本地 README 的 OpenAPI 15 QPS 边界检查批量调用。',task:'把输入按 size 切成顺序批次，作为有限并发调度基础。',sol:"export function solve({items,size}) { if(!Number.isInteger(size)||size<1) throw new TypeError('INVALID_SIZE'); const out=[]; for(let i=0;i<items.length;i+=size) out.push(items.slice(i,i+size)); return out; }",input:{items:[1,2,3,4,5],size:2},expected:[[1,2],[3,4],[5]]},
  {w:9,t:'fs、path、URL 与 process',o:['安全处理路径','理解环境变量','使用 URL 解析'],p:'本地函数运行器接受函数名并拼路径，若不校验会产生目录穿越。',m:'path.resolve/join 处理平台分隔符，但不能自动阻止越界；解析后还要确认目标位于允许根目录。process.env 的值都是字符串或 undefined。URL 类负责结构化解析，不能靠字符串 startsWith 完成安全校验。',j:'对应 Java NIO Path、System.getenv 与 URI，但 Node API 多为异步/回调和 Promise 变体。',bad:'把用户输入直接拼接到文件路径；把字符串 "false" 当成布尔 false。',crm:'阅读 `function-runner.cjs` 的函数名正则和候选路径。',task:'只允许字母数字下划线连字符的函数名。',sol:"export function solve(name) { if(!/^[A-Za-z0-9_-]+$/.test(name)) throw new TypeError('INVALID_NAME'); return name; }",input:'createCustomer',expected:'createCustomer'},
  {w:10,t:'Buffer、Stream、EventEmitter 与背压',o:['区分文本和字节','理解流式传输','识别伪流式'],p:'findStream 每批都 push 到 allRecords，最终仍因全量数据耗尽内存。',m:'Buffer 保存字节；编码决定字节与文本的转换。Stream 按块传输并通过背压协调生产消费速度。aPaaS findStream 只是分页回调，只有批内处理后丢弃数据才真正控制内存。',j:'对应 Java ByteBuffer/InputStream/Flow，但 Node 流与事件循环集成。',bad:'监听 data 后无限累积 chunk；忘记处理 error 或关闭资源。',crm:'比较 `streamQueryData` 与长任务函数的全量累积方式。',task:'返回 UTF-8 文本的字节数而不是字符数。',sol:"export function solve(text) { return Buffer.byteLength(String(text), 'utf8'); }",input:'CRM客户',expected:9},
  {w:11,t:'HTTP、进程、Worker 与性能诊断',o:['读懂 HTTP 生命周期','选择 Worker','用指标定位瓶颈'],p:'一个 CPU 密集报表让同一实例的所有 HTTP 请求卡住。',m:'HTTP 请求由方法、URL、头和体组成；响应必须有状态码、头和体。CPU 工作占据主线程时应拆给 Worker 或独立服务。先测事件循环延迟、CPU、内存和依赖耗时，再优化。',j:'Node 单进程不像传统 Servlet 线程池；一个阻塞回调就能拖慢该实例全部请求。',bad:'看到慢就增加 Promise.all；用同步 fs 和大循环处理请求。',crm:'阅读 `apaas-openapi.cjs` 的请求、超时和 Buffer 聚合。',task:'根据状态码分类 success/client/server。',sol:"export function solve(status) { if(status>=200&&status<300)return 'success'; if(status>=400&&status<500)return 'client'; if(status>=500&&status<600)return 'server'; return 'other'; }",input:503,expected:'server'},
  {w:12,t:'TypeScript 基础类型与运行时边界',o:['使用 unknown','编写类型守卫','区分编译与运行'],p:'前端把云函数返回值直接断言成 DTO，接口漂移后页面运行时报错。',m:'TypeScript 类型编译后消失。外部数据先是 unknown，通过 typeof、Array.isArray 或 schema 校验后才能进入可信领域。联合类型配合判别字段表达有限状态。',j:'类似 Java 编译类型，但没有 JVM 在反序列化时自动保留 TS interface。',bad:'使用 `as Customer` 让编译器闭嘴；在边界使用 any。',crm:'阅读 `dashboardApi.ts` 的 isRecord 和 normalizePayload。',task:'验证值是否是含非空 id/name 的客户对象。',sol:"export function solve(v) { return !!v && typeof v==='object' && !Array.isArray(v) && typeof v.id==='string' && v.id.length>0 && typeof v.name==='string' && v.name.length>0; }",input:{id:'c1',name:'客户A'},expected:true},
  {w:13,t:'泛型、高级类型与领域建模',o:['用泛型保留关系','使用判别联合','避免过度类型体操'],p:'成功和失败返回混在一个可选字段对象里，调用方无法穷尽处理。',m:'泛型描述输入输出关系，不是“任何类型”。判别联合让 success/error 分支拥有不同字段，switch/default-never 可检查新增状态。映射类型适合机械变换，不应隐藏业务语义。',j:'可类比 Java 泛型和 sealed interface，但 TS 采用结构类型。',bad:'所有 API 都返回 `Record<string, any>`；用复杂条件类型替代清晰 DTO。',crm:'阅读 dashboard 的 source loading/error/realtime 联合思路。',task:'把 `{ok,value/error}` 结果转换为稳定展示文本。',sol:"export function solve(r) { return r.ok ? `OK:${r.value}` : `ERROR:${r.error}`; }",input:{ok:false,error:'TIMEOUT'},expected:'ERROR:TIMEOUT'},
  {w:14,t:'tsconfig、CJS/ESM、npm 与工程基线',o:['读懂模块配置','使用 lockfile','解释构建目标'],p:'本地 Vite 能 import，云函数 CommonJS 部署却找不到默认导出。',m:'CJS 在运行时 require，ESM 使用静态 import。package type、扩展名、moduleResolution 和构建器共同决定解析。lockfile 固定完整依赖图，CI 使用 npm ci。target 决定输出语法，lib 决定类型可见性。',j:'类似 Maven scope、编译 target 和模块系统的组合，但 Node 同时存在两套模块格式。',bad:'随意混用 require/import；删除 lockfile 解决依赖冲突。',crm:'阅读两个 tsconfig 和 `vite.config.ts` 的 createRequire。',task:'解析 semver 主版本并判断是否为破坏性主版本差异。',sol:"export function solve({from,to}) { const major=s=>Number(/^v?(\\d+)/.exec(s)?.[1]); return major(from)!==major(to); }",input:{from:'1.4.0',to:'2.0.0'},expected:true},
  {w:15,t:'测试、调试与契约验证',o:['编写表驱动测试','选择 fake/mock','验证失败路径'],p:'改动日期映射后看板还能打开，但季度边界悄悄算错。',m:'纯函数用单元测试；跨边界用契约测试；真实数据库/缓存用容器集成测试；用户流程用 E2E。测试要覆盖边界、非法输入、依赖失败、重复请求和竞态。调试先建立可证伪假设。',j:'JUnit 思路可直接迁移；Node 内置 test runner 已足够完成基础测试。',bad:'只测 happy path；为了覆盖率 mock 掉真正需要验证的契约。',crm:'为 period、dashboardMapper 和 normalize 函数优先补测试。',task:'检查返回对象是否包含稳定的 success/data 或 success/error 契约。',sol:"export function solve(r) { if(!r||typeof r!=='object'||typeof r.success!=='boolean')return false; return r.success ? 'data' in r : typeof r.error==='string'; }",input:{success:false,error:'INVALID'},expected:true},
  {w:16,t:'aPaaS 云函数入口与元数据',o:['解释三参数入口','同步 meta 与实现','设计可测试 handler'],p:'设计器显示输出字段，函数实际返回另一种结构，前端只能运行时失败。',m:'params 是不可信输入，context 是平台身份和资源上下文，logger 是可观测接口。index.meta.json 是公开契约的一部分。把平台对象封装在 adapter 后传入 handler，纯业务逻辑才能测试。',j:'对应 Controller 方法、SecurityContext 与 Logger，但平台通过全局对象和约定注入。',bad:'相信 meta 已完成运行时校验；直接在所有辅助函数访问 global.application。',crm:'对照 `createCustomer` 的 meta、入口、normalize 和 getCustomerObject。',task:'把函数参数归一化成稳定 customer DTO。',sol:"export function solve(p={}) { const name=String(p.name??p.customerName??'').trim(); if(!name) throw new TypeError('NAME_REQUIRED'); return {name,ownerId:String(p.ownerId??'').trim()}; }",input:{customerName:' 客户A ',ownerId:7},expected:{name:'客户A',ownerId:'7'}},
  {w:17,t:'aPaaS 数据对象、字段与权限',o:['构建有界查询','理解字段形态','服务端授权'],p:'前端可传任意 objectName 和 fieldNames 调通用查询，形成数据越权。',m:'普通、枚举、关联、人员和多值字段写入形态不同。动态对象名和字段必须白名单，查询只 select 必要字段并设置范围。frontendSDKInvokable 不代表已授权，权限依据 context.user 决定。',j:'类似动态 Repository 加 Spring Security，但缺少编译期实体保护，更依赖白名单。',bad:'用前端 ownerId 代替当前身份；通用查询无最大返回量。',crm:'审查 `streamQueryData`、客户列表和权限函数。',task:'根据白名单过滤请求字段，出现未知字段即拒绝。',sol:"export function solve({fields,allowed}) { const set=new Set(allowed); if(fields.some(f=>!set.has(f))) throw new TypeError('FIELD_FORBIDDEN'); return [...new Set(fields)]; }",input:{fields:['name','owner','name'],allowed:['name','owner']},expected:['name','owner']},
  {w:18,t:'流式、批量、长任务与幂等',o:['真正流式处理','设计批次结果','保证重复执行安全'],p:'数据修复执行到一半超时，重跑后部分记录被重复追加。',m:'findStream 控制读取批次，batchUpdate 减少往返，长任务扩展时间窗口；三者解决不同问题。每批记录 checkpoint、成功数、失败业务键。写操作使用唯一约束、幂等键或状态机。',j:'类似 Spring Batch 的 chunk、JobRepository 和 restartability。',bad:'全量 push 后再处理；用“先查再建”当作并发幂等。',crm:'阅读 batchStreamUpdate 和区域运营长任务。',task:'为记录生成稳定批次，每批带序号和 items。',sol:"export function solve({items,size}) { if(size<1)throw new TypeError('SIZE'); const out=[]; for(let i=0;i<items.length;i+=size)out.push({batch:i/size+1,items:items.slice(i,i+size)}); return out; }",input:{items:['a','b','c'],size:2},expected:[{batch:1,items:['a','b']},{batch:2,items:['c']}]},
  {w:19,t:'配置、metadata、文件与函数编排',o:['管理服务端配置','缓存元数据','安全生成文件'],p:'导出 Excel 把用户输入的 `=...` 原样写入，打开时可能执行公式。',m:'globalVar 存环境配置而非业务状态。metadata 查询代价高且会变化，可短期缓存但要失效。文件生成限制行数/大小，转义公式前缀，上传后清理临时文件。函数编排要传播 requestId 和错误。',j:'类似 Spring Configuration、元数据缓存和批处理资源生命周期。',bad:'把 API Key 返回前端；把临时路径永久保存；循环内反复查 metadata。',crm:'阅读 exportOpportunityToExcel、queryTableStructure 和函数间 invoke。',task:'转义可能触发 Excel 公式的文本。',sol:"export function solve(v) { const s=String(v??''); return /^[=+\\-@]/.test(s) ? `'${s}` : s; }",input:'=1+1',expected:"'=1+1"},
  {w:20,t:'连接器、身份与跨系统一致性',o:['区分身份标识','设计 Outbox','处理部分失败'],p:'CRM 保存成功、致远超时；用户重试后两边可能重复或不一致。',m:'跨系统没有天然事务。先在本地事务记录业务变化和 outbox，再异步发送；消费者按幂等键处理，失败退避并进入死信，定期对账。open_id、CRM user id、邮箱和员工号必须显式转换。',j:'对应事务消息、Saga 和防腐层，而不是强行使用 XA。',bad:'外部失败就假装回滚本地；把不同系统 userId 混用。',crm:'阅读 syncOpportunityTo/FromSeeyon、batchGetLarkUserOpenId。',task:'把同步事件状态按合法迁移推进。',sol:"export function solve({state,event}) { const m={pending:{send:'sending'},sending:{ok:'done',fail:'retry'},retry:{send:'sending'}}; const next=m[state]?.[event]; if(!next)throw new TypeError('INVALID_TRANSITION'); return next; }",input:{state:'sending',event:'fail'},expected:'retry'},
  {w:21,t:'浏览器、DOM、HTTP 与安全边界',o:['理解 DOM 事件','解释 CORS','防止 XSS'],p:'智能体 Markdown 被直接写入 innerHTML，恶意内容可能执行脚本。',m:'浏览器有 DOM、事件循环和同源策略。CORS 是浏览器读取限制，不是服务端鉴权。任何进入 HTML 的不可信内容都要按上下文转义或使用成熟 sanitizer；服务端密钥永远不进 bundle。',j:'后端模板转义经验可迁移，但 React 默认只转义文本，dangerouslySetInnerHTML 明确绕过保护。',bad:'用正则删除 script 标签当 HTML 清洗；认为 CORS 能阻止 curl。',crm:'审查 `agent_input` 的 Showdown 与 dangerouslySetInnerHTML。',task:'对纯文本做最小 HTML 转义。',sol:"export function solve(s) { return String(s).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[c])); }",input:'<b>客户&商机</b>',expected:'&lt;b&gt;客户&amp;商机&lt;/b&gt;'},
  {w:22,t:'React 组件、状态、表单与 Effect',o:['区分 props/state','写受控表单','清理副作用'],p:'级联组件同时从 props、model 和内部 state 取值，更新后相互覆盖。',m:'render 应是 props/state 到 UI 的纯计算。Effect 只同步外部系统并声明依赖，返回清理函数。受控输入只有一个真相源；状态更新用新引用。Strict Mode 会帮助暴露不安全副作用。',j:'组件 render 不是传统 Controller 方法；它可重复执行，不能在其中做写操作。',bad:'Effect 中无条件 setState 导致循环；用 index 当可变列表 key。',crm:'阅读 PC/移动 cascader 的 model、useViewModel 和 effect。',task:'实现表单 reducer，支持 change 和 reset 且不修改原对象。',sol:"export function solve({state,action}) { if(action.type==='change')return {...state,[action.name]:action.value}; if(action.type==='reset')return {}; throw new TypeError('ACTION'); }",input:{state:{name:'A'},action:{type:'change',name:'name',value:'B'}},expected:{name:'B'}},
  {w:23,t:'React 数据请求、竞态与测试',o:['建模加载状态','取消旧请求','按用户行为测试'],p:'用户快速切换周期，旧请求最后返回并覆盖新看板。',m:'请求状态至少包含 idle/loading/success/empty/error。用 AbortController 取消旧请求，或用递增 requestId 只接受最新响应。组件测试点击和可见结果，不读取 Hook 内部状态。',j:'类似后端请求版本控制，但竞态发生在一个页面的多个异步生命周期。',bad:'只用一个 loading 布尔值；catch 后展示旧权限数据。',crm:'阅读 `useDashboardData` 的 requestRef 和错误空态。',task:'从响应列表中只接收 requestId 最大的一项。',sol:"export function solve(rows) { return rows.reduce((best,r)=>!best||r.requestId>best.requestId?r:best,null)?.data ?? null; }",input:[{requestId:2,data:'new'},{requestId:1,data:'old'}],expected:'new'},
  {w:24,t:'aPaaS 组件、AntD、ECharts 与联调',o:['连接 ViewModel','管理图表生命周期','验证前后端契约'],p:'筛选变化后图表残留旧实例，接口 v2/v3 混用导致白屏。',m:'aPaaS model 暴露平台可绑定状态，React 负责展示；两者边界要明确。ECharts 创建后监听 resize，卸载 dispose。API 返回先验证 contractVersion 和字段，再映射为视图模型。',j:'ViewModel 类似受平台管理的 Bean，但生命周期与 React tree 不完全一致。',bad:'组件直接依赖原始后端字段；每次 render 初始化图表。',crm:'沿 eopDashboardData → dashboardApi → mapper → charts 阅读。',task:'把扁平产品路径构建成稳定级联树。',sol:"export function solve(paths) { const root=[]; for(const path of paths){let level=root; for(const value of path){let n=level.find(x=>x.value===value); if(!n){n={value,label:value,children:[]};level.push(n);} level=n.children;}} return root; }",input:[['A','A1'],['A','A2']],expected:[{value:'A',label:'A',children:[{value:'A1',label:'A1',children:[]},{value:'A2',label:'A2',children:[]}]}]},
  {w:25,t:'NestJS 模块、Controller、Provider 与 DI',o:['建立模块边界','使用构造器注入','保持 Controller 薄'],p:'云函数把校验、查询、映射、外部调用全写在一个 600 行入口里。',m:'Module 声明边界，Controller 处理协议，Provider 实现用例，Repository/Adapter 隔离技术依赖。依赖从外向内，领域逻辑不 import Nest 或 Prisma。',j:'Module、Controller、Injectable 与 Spring Configuration、RestController、Service 高度相似。',bad:'把业务写在 Controller；使用全局单例绕过 DI。',crm:'把 createCustomer 拆成 handler、use case、repository adapter 的设计练习。',task:'通过注入的 repository 创建客户，证明业务不依赖全局对象。',sol:"export function solve({repo,input}) { if(!input.name?.trim())throw new TypeError('NAME'); return repo.create({name:input.name.trim()}); }",input:{repo:'CustomerRepository',input:{name:' 客户A '}},inputExpr:"{repo:{create:v=>({id:'c1',...v})},input:{name:' 客户A '}}",expected:{id:'c1',name:'客户A'}},
  {w:26,t:'DTO、Pipe、Guard、Interceptor 与错误',o:['运行时校验 DTO','区分认证授权','统一错误响应'],p:'接口返回 500 和底层数据库错误，用户不知道如何处理，日志也无法关联请求。',m:'Pipe 转换校验输入，Guard 决定能否访问，Interceptor 包裹调用做计时/转换，Exception Filter 统一协议错误。错误包含稳定 code、用户 message 和 requestId，内部 cause 仅入受控日志。',j:'对应 Bean Validation、Security Filter、HandlerInterceptor 和 ControllerAdvice。',bad:'所有异常返回 200；只在前端隐藏无权限按钮。',crm:'将 checkUserPermission 规则移动到服务端授权用例思考。',task:'按角色和动作执行最小 RBAC 判断。',sol:"export function solve({roles,action}) { const grants={admin:['read','write'],sales:['read','write'],viewer:['read']}; return roles.some(r=>grants[r]?.includes(action)); }",input:{roles:['viewer'],action:'write'},expected:false},
  {w:27,t:'PostgreSQL、Prisma、事务与迁移',o:['设计关系模型','划分事务边界','安全迁移'],p:'客户创建与 outbox 事件分两次写入，进程在中间崩溃导致永不发送。',m:'事务保证一个数据库内原子性；业务记录与 outbox 同事务写入。索引服务于真实查询，唯一约束承担并发正确性。迁移采用 expand-and-contract：先新增兼容，再回填切换，最后删除。',j:'事务和迁移原则与 Spring/JPA 相同；Prisma model 是基础设施模型，不应直接充当所有 API DTO。',bad:'用应用层查重代替唯一约束；一步重命名生产大字段。',crm:'对 createCustomer 的名称/编码查重分析竞态。',task:'生成事务内应执行的步骤序列。',sol:"export function solve(command) { return [{op:'insertCustomer',id:command.id},{op:'insertOutbox',key:`customer:${command.id}:created`}]; }",input:{id:'c1'},expected:[{op:'insertCustomer',id:'c1'},{op:'insertOutbox',key:'customer:c1:created'}]},
  {w:28,t:'OIDC/JWT、RBAC、OpenAPI 与演进',o:['区分认证和授权','验证 Token','保持 API 兼容'],p:'服务相信 JWT payload 却不验证签名、issuer、audience 和过期时间。',m:'OIDC 解决身份，OAuth2 授权访问，JWT 只是令牌格式。验证签名算法、iss、aud、exp，并把外部 subject 映射到内部用户。OpenAPI 描述协议；演进优先新增可选字段，破坏变更给版本和废弃期。',j:'对应 Spring Security Resource Server 与 springdoc-openapi。',bad:'decode 等于 verify；角色只存在前端；随意修改字段含义。',crm:'整理 open_id、CRM _user id、邮箱和员工号的映射边界。',task:'检查 claims 的 issuer、audience 和 exp。',sol:"export function solve({claims,now,issuer,audience}) { return claims.iss===issuer && claims.aud===audience && Number(claims.exp)>now; }",input:{claims:{iss:'idp',aud:'crm',exp:200},now:100,issuer:'idp',audience:'crm'},expected:true},
  {w:29,t:'Redis 缓存、限流与一致性',o:['设计 cache-aside','处理击穿','理解分布式锁边界'],p:'产品级联每次请求都全表查询，缓存后数据更新又长期不刷新。',m:'cache-aside 先读缓存，未命中查源并写 TTL；写操作删除或更新缓存。key 包含租户和版本。防穿透可缓存短期空值，防击穿可单飞或互斥，防雪崩给 TTL 抖动。',j:'与 Spring Cache/RedisTemplate 原理一致，但不能把注解当成一致性保证。',bad:'永久缓存；使用 KEYS 扫生产；锁无超时和所有权。',crm:'为 queryProductCascade、metadata 和统称列表设计缓存。',task:'生成包含租户、资源、版本的缓存 key。',sol:"export function solve({tenant,resource,version='v1'}) { for(const v of [tenant,resource,version])if(!v)throw new TypeError('KEY'); return `crm:${tenant}:${resource}:${version}`; }",input:{tenant:'t1',resource:'products',version:'v2'},expected:'crm:t1:products:v2'},
  {w:30,t:'BullMQ、消息、Outbox 与可靠消费',o:['实现至少一次消费','设计退避死信','保证消费者幂等'],p:'同步消息在消费者成功写入后、ack 前断线，消息再次投递。',m:'可靠队列通常提供 at-least-once，重复是正常事件。消费者用业务幂等键或状态表去重。重试只针对瞬态错误，超过上限进死信；Outbox relay 把数据库事件发送到队列。',j:'对应 Spring Kafka/Rabbit listener 与事务 Outbox；不要声称端到端 exactly-once。',bad:'把消息 ID 当业务幂等键；无限重试毒消息。',crm:'把致远双向同步改画为 outbox→worker→对账。',task:'生成指数退避毫秒数并设上限。',sol:"export function solve({attempt,base=1000,max=30000}) { return Math.min(max,base*(2**Math.max(0,attempt-1))); }",input:{attempt:6,base:1000,max:30000},expected:30000},
  {w:31,t:'可观测、安全、容器与交付',o:['使用 RED 指标','传播 traceId','构建安全流水线'],p:'用户说“同步失败”，系统只有一条无 requestId 的 error.message。',m:'日志回答发生了什么，指标回答是否普遍，Trace 回答时间花在哪里。记录 requestId、操作、耗时、数量和错误码并脱敏。CI 依次安装、静态检查、测试、构建、扫描；镜像不可变，发布使用灰度和可回滚迁移。',j:'对应 SLF4J/Micrometer/Actuator 与成熟 Java 交付链。',bad:'日志打印完整对象和 Token；只监控 CPU；latest 镜像直接覆盖生产。',crm:'为 eopDashboardData 和同步函数设计 RED 指标与 runbook。',task:'递归脱敏日志对象中的 token、secret、password。',sol:"export function solve(v) { const secret=/token|secret|password/i; if(Array.isArray(v))return v.map(solve); if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,secret.test(k)?'[REDACTED]':solve(x)])); return v; }",input:{user:'u1',token:'abc',nested:{password:'p'}},expected:{user:'u1',token:'[REDACTED]',nested:{password:'[REDACTED]'}}},
  {w:32,t:'毕业项目：企业级 CRM 协同系统',o:['串联全栈架构','用验收证明可靠性','完成故障演练'],p:'把“能运行的 Demo”升级为重复请求不重、依赖失败可恢复、越权不可读、线上问题可定位的系统。',m:'毕业项目从用户旅程和 SLO 开始，先定义契约、数据与失败模式，再实现 UI/API/任务。所有关键边界都有测试和观测。部署不是结束，还要演练消息重放、依赖超时、缓存失效和迁移回滚。',j:'把你在 Java 企业项目中的工程纪律迁移到 Node，同时尊重事件循环、JS 类型和前端生命周期。',bad:'一次性堆满技术栈却没有验收；把能启动当成完成。',crm:'选择客户创建→确认卡→同步致远或 EOP 看板作为端到端参照。',task:'根据检查项计算毕业项目是否达到发布门槛。',sol:"export function solve(c) { const required=['tests','auth','idempotency','observability','rollback']; const missing=required.filter(k=>c[k]!==true); return {ready:missing.length===0,missing}; }",input:{tests:true,auth:true,idempotency:true,observability:true,rollback:true},expected:{ready:true,missing:[]}},
];
