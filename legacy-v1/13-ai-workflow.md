# 13 AI 辅助全栈研发方法

## 正确分工

AI 擅长搜索、解释、生成候选、补测试、归纳日志和机械重构；人负责目标、约束、风险、架构取舍、权限和最终验证。AI 输出不是事实，必须落到源码、文档、测试或运行结果。

## 六步协作流程

1. 给上下文：业务目标、入口、相关文件、运行时、不能改的边界。
2. 先让 AI 建调用链和假设表，不立即改代码。
3. 要求最小变更计划、失败模式和验收条件。
4. 先生成能复现问题的测试，再修改。
5. 运行 typecheck/test/build，人工检查 diff 和安全边界。
6. 让 AI 反向审查：哪里可能错、依据是什么、尚未验证什么。

## 可复用提示词

### 解释函数

```text
你是我的结对工程师。我有 Java/Spring 背景。请按“入口契约→数据流→异步边界→平台 API→失败模式→Java 类比”解释这个云函数。每个结论引用具体代码；未知处明确写未知。
```

### 生成测试

```text
为该纯函数列出等价类、边界值和异常路径，再生成表驱动测试。不要改实现。重点覆盖 null/undefined、空数组、重复值、时区边界和外部数据类型漂移。
```

### 安全评审

```text
只做审查，不改代码。按越权、注入、SSRF、XSS、敏感日志、资源耗尽、依赖风险检查；每项给证据、可利用条件、影响和最小修复。
```

### 调试

```text
根据错误、日志与调用链建立最多 5 个可证伪假设，按概率和验证成本排序；每个假设给一条只读验证命令。不要先猜修复。
```

## 上下文与隐私

只提供必要文件；Token、Cookie、客户数据、手机号、邮箱先脱敏。让 AI 使用接口/fixture 而不是真实生产数据。生成 SQL、批量更新、迁移或删除命令时必须先 dry-run、核对作用范围并准备恢复方案。

## AI 学习闭环

每周选择一个函数：自己先画数据流；让 AI 解释；找出差异；写测试验证；最后不用 AI 口述设计。若只能“看懂 AI 答案”而不能预测代码行为，说明还未掌握。

