# 实验使用说明

每周先进入 `labs/week-XX` 阅读 README，再运行 `node --test`。starter 的关键测试失败是预期行为；完成 TODO 后应全部通过。卡住 30 分钟后才查看 `solutions/week-XX`。

根目录 `npm run test:solutions` 验证所有参考答案。实验只使用脱敏 CRM fixture，不需要飞书或生产凭证。
